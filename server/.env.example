# DATABASE_URL points Prisma at your local PostgreSQL database.
# Create the database first:  createdb fswd_ca2
# Then copy this file to .env and fill in your PostgreSQL username/password.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/fswd_ca2?schema=public"
